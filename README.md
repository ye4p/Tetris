# Tetris

To compile: gcc main.c -o main

# Controls: w to rotate, a and d to move shapes to left and right, s to go down

# Compact version of tetris in C around ~500 lines of code. Has score tracking system, rotation support, bounds checking, layer clearing
